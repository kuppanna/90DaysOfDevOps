name: Hello workflow    
on:
  push:
jobs:
    greet:
        runs-on: ubuntu-latest
        steps:
            - name: checkout code
              uses: actions/checkout@v4
            - name: print messages
              run: echo "Hello from Github actions!"

      